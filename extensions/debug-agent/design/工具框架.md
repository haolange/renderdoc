
> 状态：设计草案（Draft）。本文档用于描述工具层的设计目标与分层思路，可能包含未完成的章节或占位内容。
> 实际可用的工具名称以 rdx-mcp 暴露的 `rd.*` 工具集为准；框架层约束以 `../common/` 下规范为准。

Renderdoc Debug Agent 工具框架
遵照“为 Agent 提供与人类专家通过 GUI 操作等价的 API”这一核心思想，本报告对 RenderDoc 的全部 UI 功能进行了地毯式分析与映射，旨在构建一套真正全面、强大且具备第一性原理的 MCP (Model Context Protocol) 工具集，确保 AI Agent 能够复现人类专家在 RenderDoc UI 上的任何调试操作。
# 一，设计哲学
## 1.1，目标
本工具集的设计旨在实现两大核心目标，以赋能 Agent 在 RenderDoc 生态系统中的调试能力：
能力对齐 RenderDoc GUI：确保 Agent 无需模拟图形用户界面 (GUI) 的鼠标操作，即可获取与 GUI 面板等效的 state、resource、history 和 debug 数据及其导出物。
补齐人类专家的“组合技”：鉴于 GUI 仅提供可视化信息，而人类专家常依赖固定的排查流程（例如：pixel history → state diff → resource lifetime → shader hotfix），本工具集在原子工具之上增设了 Diagnostics 和 Macro Workflows 两层工具。此举旨在为 Agent 提供可复用、可解释且可验证的排查机制。
## 1.2，原则
本工具集遵循以下五项关键设计原则，以确保其高效性、可维护性和可扩展性：
显式句柄管理 (Explicit Handles)：所有核心对象（如 capture file、replay session、remote connection、shader debug session）均通过其对应的 *_id 进行显式引用，从而避免了隐式全局状态可能导致的混淆和错误。
大数据一律走 Artifact (Files as Artifacts)：对于texture、buffer、mesh、shader binary 等大型数据对象，默认将其导出为文件并返回相应的 *_path。得益于Native Multi Modality，此策略有效避免了 JSON 响应过大，提升了数据传输效率。
原子化 + 正交性 (Primitives + Orthogonality)：基础工具被设计为尽可能原子化，以提供高度的灵活性。同时，宏工具在输出结果的同时提供 trace（内部关键步骤），确保操作的可审计性和可复现性。
跨 API 的“共同语义”优先：在不同图形 API（如 D3D12/Vulkan）之间，同名工具力求返回统一的语义。特定于 API 的字段则被封装在 api_specific 子字段中，以保持接口的简洁性和一致性。
“能解释”是第一等公民：Diagnostics 和 Macro 工具的输出不仅提供数据，更重要的是提供 why（结构化证据和建议的下一步行动），显著降低了 Agent 做出错误判断的概率。

# 二，通用数据结构约定
## 2.1，句柄与标识

## 2.2，返回值通用约定
除特别说明外，所有工具的返回值均包含以下字段：
success (bool)：指示操作是否成功完成。
error_message (str, optional)：若操作失败，提供详细的失败原因。
当返回的数据量可能非常大时，工具将优先返回 *_path（即 artifact 文件的路径），而非直接在响应中内联数据，以优化性能和传输效率。

# 三，工具总览
RenderDoc 工具集共包含 196 个工具，其设计遵循清晰的分层架构，以满足不同粒度的调试与分析需求：
Core/Replay/Inspect（原子工具）：这些工具旨在提供与 RenderDoc GUI 面板一致的数据访问能力，是进行底层操作的基础。
Diagnostics（启发式诊断）：此层工具将常见的性能瓶颈和错误模式固化为可复用的检查项，帮助用户快速定位问题。
Macro Workflows（专家工作流）：该层工具将专家级的排查流程和最佳实践封装成闭环的工作流，以自动化复杂问题的诊断过程。
## 3.1，核心与环境管理 (Core & Environment)


## 3.2，捕获文件与 Replay Session (Capture & Replay)


## 3.3，事件导航与 API 检查 (Event Navigation & API Inspection)


## 3.4，管线状态查询 (Pipeline State Inspection)


## 3.5，资源探查 (Resource Inspection & History)


## 3.6，纹理数据访问与分析 (Texture Data Access & Analysis)


## 3.7，缓冲区与网格数据访问 (Buffer & Mesh Data Access)


## 3.8，Shader 探查、反射与替换 (Shader Inspection & Replacement)
以下表格详细列出了用于 Shader 探查、反射与替换的各项工具及其功能、参数与返回结果：


## 3.9，Shader Debugger 与像素/顶点级调试 (Shader Debugging)


## 3.10，性能与高级分析 (Performance & Advanced Analysis)


## 3.11，导出、报告与可复现打包 (Export & Reporting)


## 3.12，远程连接与自动化抓帧 (Remote Capture & Target Control)
以下表格详细阐述了用于远程连接 RenderDoc 服务器及进行自动化抓帧的关键工具及其功能、参数与返回结果。


## 3.13，应用内接口抽象 (In-Application Instrumentation, Optional)


## 3.14，诊断工具 (Diagnostics, Heuristic Checks)


## 3.15，专家级宏工作流 (Expert Macro Workflows)


## 3.16，通用辅助工具 (Utilities, Optional)

# 四，总结
本文通过对 RenderDoc UI 的精细解构和功能映射，详细抽象并实现了Renderdoc MCP，使其从“可用”到“专家级”的跨越。它为 AI Agent 提供了前所未有的对Renderdoc的控制粒度，使其能够模拟人类专家的完整调试流程，包括复杂的视图交互、状态设置和多窗口协同。这套工具集是实现真正智能图形调试 Agent 的坚实基础。



| 名称 | 类型 | 说明 |
| capture_file_id | str | 由 rd.capture.open_file 返回的捕获文件句柄 |
| session_id | str | 由 rd.capture.open_replay 返回的 Replay Session 句柄 |
| remote_id | str | 由 rd.remote.connect 返回的远程连接句柄 |
| shader_debug_id | str | 由 rd.shader.debug_start 返回的 shader debug 句柄 |
| resource_id / texture_id / buffer_id / shader_id | str | 资源标识 (RenderDoc ResourceId 的可序列化形式) |
| event_id | int | RenderDoc eventId (Replay 时间轴核心坐标) |
| frame_index | int | 帧索引 (从 0 或 1 开始由实现约定；建议 0-based) |
| stage | str | Shader stage：VS/HS/DS/GS/PS/CS/AS/MS/RT (按平台能力可裁剪) |
| subresource | dict | 纹理子资源：{mip:int, slice:int, sample:int} |
| rect | dict | 矩形区域：{x:int, y:int, w:int, h:int} |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.core.init | 初始化 RenderDoc Replay/Remote 运行时；建立对象池与临时目录；必须在首次使用前调用一次。 | global_env (dict, 可选): 全局配置，例如 {temp_dir, artifact_dir, log_level, replay_cache_size_mb}<br>enable_remote (bool, 可选, 默认 true): 是否启用 rd.remote.*<br>enable_app_api (bool, 可选, 默认 false): 是否启用 rd.app.*（需要目标进程内集成 App API） | success (bool)<br>api_version (str): RenderDoc API 版本（如 '1.xx'）<br>capabilities (dict): 支持的图形 API / 平台能力摘要<br>error_message (str, 可选) |
| rd.core.shutdown | 关闭运行时并释放所有资源（Replay Session、Remote 连接、临时文件句柄）。 | 无 | success (bool)<br>released (dict): {sessions, capture_files, remote_connections, temp_files}<br>error_message (str, 可选) |
| rd.core.get_version | 获取 RenderDoc 运行时版本与构建信息。 | 无 | success (bool)<br>version (str)<br>commit_hash (str, 可选)<br>build_date (str, 可选)<br>error_message (str, 可选) |
| rd.core.get_capabilities | 获取当前环境能力详情（Replay、远程抓帧、shader debug、counter 支持等）。 | detail_level (str, 可选, 默认 'summary'): 'summary' | 'full' | success (bool)<br>capabilities (dict): 例如 {replay: {...}, remote: {...}, shader_debug: {...}, counters: {...}}<br>error_message (str, 可选) |
| rd.core.set_config | 设置运行时配置（对后续调用生效）。 | config (dict): {artifact_dir?, temp_dir?, max_artifact_bytes?, default_image_format?, default_mesh_format?, log_level?} | success (bool)<br>applied (dict): 实际生效的配置（包含默认值补全）<br>error_message (str, 可选) |
| rd.core.get_config | 读取当前运行时配置。 | 无 | success (bool)<br>config (dict)<br>error_message (str, 可选) |
| rd.core.set_log_level | 设置内部日志等级，便于定位 MCP Server 与 RenderDoc 侧错误。 | level (str): 'trace'|'debug'|'info'|'warn'|'error' | success (bool)<br>error_message (str, 可选) |
| rd.core.get_logs | 读取内部日志（可按时间/级别过滤）。 | since_ms (int, 可选): 仅返回该时间戳之后的日志<br>level_min (str, 可选): 最低级别过滤<br>max_lines (int, 可选, 默认 500) | success (bool)<br>logs (list[dict]): 每条包含 {ts_ms, level, message, context?}<br>error_message (str, 可选) |
| rd.core.healthcheck | 执行自检：验证 RenderDoc 库加载、临时目录写入、（可选）创建最小 Replay/Remote 连接等。 | check_remote (bool, 可选, 默认 false)<br>check_replay (bool, 可选, 默认 true) | success (bool)<br>checks (list[dict]): {name, ok, detail?}<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.capture.open_file | 打开一个 .rdc 捕获文件并返回文件句柄（不自动创建 Replay）。 | file_path (str): 捕获文件的绝对/可访问路径<br>read_only (bool, 可选, 默认 true): 是否以只读方式打开 | success (bool)<br>capture_file_id (str): 捕获文件句柄 ID<br>driver (str): 捕获时使用的图形 API（如 'D3D12'/'Vulkan'）<br>error_message (str, 可选) |
| rd.capture.close_file | 关闭捕获文件句柄。 | capture_file_id (str) | success (bool)<br>error_message (str, 可选) |
| rd.capture.get_info | 获取捕获文件元数据（时间、可执行文件、驱动、机器信息、RDC 版本等）。 | capture_file_id (str) | success (bool)<br>metadata (dict): {capture_time, executable, cmdline, api, driver_version, machine_name, notes?}<br>error_message (str, 可选) |
| rd.capture.get_thumbnail | 获取捕获文件缩略图（如果存在）。 | capture_file_id (str)<br>max_size_px (int, 可选, 默认 256): 缩略图最大边长 | success (bool)<br>image_path (str, 可选): 缩略图保存路径（PNG/JPEG）<br>width (int, 可选)<br>height (int, 可选)<br>error_message (str, 可选) |
| rd.capture.list_frames | 列出捕获文件中所有帧的基本信息（帧索引、时间戳、缩略图可用性等）。 | capture_file_id (str) | success (bool)<br>frames (list[dict]): 每个元素 {frame_index, timestamp?, has_thumbnail?, drawcall_count?}<br>error_message (str, 可选) |
| rd.capture.open_replay | 基于 capture_file_id 创建一个可回放的 Replay Session（会启动 replay driver）。 | capture_file_id (str)<br>options (dict, 可选): 回放选项，例如 {force_api?, gpu_id?, software_replay?, enable_debug?, replay_cache_mb?} | success (bool)<br>session_id (str): Replay Session 句柄<br>frame_count (int)<br>api_properties (dict): API 特性与限制<br>error_message (str, 可选) |
| rd.capture.close_replay | 关闭一个 Replay Session，释放回放 driver 与缓存资源。 | session_id (str) | success (bool)<br>error_message (str, 可选) |
| rd.replay.set_frame | 切换当前回放帧（会重置 active event 到该帧起始）。 | session_id (str)<br>frame_index (int) | success (bool)<br>active_event_id (int): 切换后默认激活的 eventId（通常为该帧起始）<br>error_message (str, 可选) |
| rd.replay.get_frame_info | 获取当前/指定帧的统计信息（drawcall 数、marker 数、时长等）。 | session_id (str)<br>frame_index (int, 可选): 缺省为当前帧 | success (bool)<br>frame_info (dict): {frame_index, event_range:{start,end}, drawcall_count, marker_count, duration_ms?}<br>error_message (str, 可选) |
| rd.event.set_active | 设置当前 active event（后续 pipeline/resource/debug 查询均以该 event 为上下文）。 | session_id (str)<br>event_id (int) | success (bool)<br>active_event_id (int)<br>error_message (str, 可选) |
| rd.event.get_active | 读取当前 active event。 | session_id (str) | success (bool)<br>active_event_id (int)<br>error_message (str, 可选) |
| rd.replay.get_api_properties | 获取 Replay Session 的 API 属性（如 shader debug 支持、counter 支持等）。 | session_id (str) | success (bool)<br>api_properties (dict)<br>error_message (str, 可选) |
| rd.replay.get_driver_info | 获取回放 driver 信息（版本、vendor、device、driver 字符串等）。 | session_id (str) | success (bool)<br>driver_info (dict): {vendor, device, driver_version, driver_name, replay_api}<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.event.get_actions | 用于获取当前帧的根 Action 列表，进而构建 Drawcall/Marker 树。 | session_id (str)<br>include_markers (bool, 可选, 默认 true)<br>include_drawcalls (bool, 可选, 默认 true) | success (bool)<br>actions (list[dict]): 树结构节点，包含常用字段如 {event_id, name, flags, num_indices, num_instances, children?}<br>error_message (str, 可选) |
| rd.event.get_action_tree | 获取当前帧完整的 Action 树，支持按深度或过滤条件进行裁剪。 | session_id (str)<br>max_depth (int, 可选): 限制树的深度<br>filter (dict, 可选): 过滤条件，例如 {name_regex?, flags_include?, flags_exclude?, event_id_range?} | success (bool)<br>root (dict): 根节点（包含 children 列表）<br>error_message (str, 可选) |
| rd.event.get_action_details | 获取单个 Action 的详细信息，包括 Drawcall 参数、Marker 范围以及输出目标等摘要。 | session_id (str)<2>event_id (int) | success (bool)<br>action (dict): 包含字段如 {event_id, name, flags, outputs?, num_vertices?, num_indices?, num_instances?, topology?, dispatch_dims?, ...}<br>error_message (str, 可选) |
| rd.event.get_drawcall_children | 获取指定 Event 对应 Action 的子节点 event_id 列表，便于快速遍历。 | session_id (str)<br>event_id (int) | success (bool)<br>children_event_ids (list[int])<br>error_message (str, 可选) |
| rd.event.get_parent_chain | 获取指定 Event 的父链（从根节点到当前 Event），用于还原 Marker Stack 或 Pass 层级。 | session_id (str)<br>event_id (int) | success (bool)<br>parent_chain (list[dict]): 每层包含 {event_id, name, flags}<br>error_message (str, 可选) |
| rd.event.search_actions | 在当前帧的 Action 树中搜索节点，支持按名称、Flag 或事件范围等条件。 | session_id (str)<br>query (dict): 查询条件，例如 {name_regex?, name_contains?, flags_include?, flags_exclude?, event_id_min?, event_id_max?}<br>max_results (int, 可选, 默认 200) | success (bool)<br>matches (list[dict]): 匹配结果，包含 {event_id, name, flags, path?}（path 为父链名称数组）<br>error_message (str, 可选) |
| rd.event.list_passes | 根据 Marker 或 Region 归纳当前帧的 Pass 列表，类似于 RenderDoc Event Browser 的 Region 视图。 | session_id (str)<br>marker_policy (str, 可选, 默认 'region'): 可选值 'region', 'markers_only', 'both' | success (bool)<br>passes (list[dict]): 包含字段如 {name, begin_event_id, end_event_id, drawcall_count, children?}<br>error_message (str, 可选) |
| rd.event.get_marker_stack | 获取指定 Event 时刻的 Marker Stack（从外层到内层）。 | session_id (str)<br>event_id (int) | success (bool)<br>stack (list[str])<br>error_message (str, 可选) |
| rd.event.get_api_calls | 获取指定 Event 对应的底层 API 调用列表，例如 D3D 或 Vulkan 命令。 | session_id (str)<br>event_id (int)<br>include_arguments (bool, 可选, 默认 true)<br>max_calls (int, 可选, 默认 2000) | success (bool)<br>api_calls (list[dict]): 包含字段如 {index, name, args?, ret?}<br>error_message (str, 可选) |
| rd.event.get_callstack | 获取指定 Event 的 CPU Callstack（如果捕获时包含该信息且平台支持）。 | session_id (str)<br>event_id (int)<br>resolve_symbols (bool, 可选, 默认 false) | success (bool)<br>callstack (list[dict]): 包含字段如 {module, function, file?, line?, address}<br>error_message (str, 可选) |
| rd.event.diff_pipeline_state | 对比两个 Event 的 Pipeline State 差异，可用于定位“状态被谁修改”的问题。 | session_id (str)<br>event_a (int): 基准 eventId<br>event_b (int): 对比 eventId<br>scope (str, 可选, 默认 'all'): 对比范围，可选值 'all', 'shaders', 'bindings', 'raster', 'om', 'ia'<br>include_unchanged (bool, 可选, 默认 false) | success (bool)<br>diff (list[dict]): 每项包含 {path, before, after, severity?}<br>error_message (str, 可选) |
| rd.event.get_resource_usage | 获取指定 Event 上资源的摘要使用情况，例如哪些资源被绑定为 RT/DS/SRV/UAV/CBV/IB/VB 等。 | session_id (str)<br>event_id (int) | success (bool)<br>usage (dict): 包含字段如 {render_targets, depth_target, vertex_buffers, index_buffer, per_stage_bindings...}<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.pipeline.get_state | 获取当前 active event 的完整 Pipeline State（返回结构按 API 类型组织）。 | session_id (str)<br>detail_level (str, 可选, 默认 'full'): 'summary'|'full' | success (bool)<br>pipeline_state (dict): 包含 IA/Shader Stages/RS/OM 等<br>error_message (str, 可选) |
| rd.pipeline.get_state_summary | 获取 Pipeline State 的紧凑摘要（适合大模型快速阅读）。 | session_id (str)<br>include_bindings (bool, 可选, 默认 true)<br>include_shaders (bool, 可选, 默认 true) | success (bool)<br>summary (dict): {ia, shaders, raster, om, descriptors?, warnings?}<br>error_message (str, 可选) |
| rd.pipeline.get_stage_state | 获取指定 Shader Stage 的 Pipeline 子状态。 | session_id (str)<br>stage (str): 'VS'|'HS'|'DS'|'GS'|'PS'|'CS'|'AS'|'MS'|'RT' | success (bool)<br>stage_state (dict): {shader, entry, resources, samplers, constant_blocks, UAVs, ...}<br>error_message (str, 可选) |
| rd.pipeline.get_vertex_input | 获取 Input Assembler 状态：VB/IB、stride/offset、顶点布局/attribute 映射。 | session_id (str) | success (bool)<br>ia (dict): {topology, vertex_buffers, index_buffer, input_layout/attributes, primitive_restart?}<br>error_message (str, 可选) |
| rd.pipeline.get_vertex_buffers | 获取当前绑定的 Vertex Buffers 列表（含 slot/stride/offset/resource_id）。 | session_id (str) | success (bool)<br>vertex_buffers (list[dict]): {slot, resource_id, offset, stride, byte_size?, name?}<br>error_message (str, 可选) |
| rd.pipeline.get_index_buffer | 获取当前绑定的 Index Buffer（resource_id/format/offset）。 | session_id (str) | success (bool)<br>index_buffer (dict): {resource_id, format, offset, name?}<br>error_message (str, 可选) |
| rd.pipeline.get_primitive_topology | 获取拓扑信息（Primitive Topology、Patch Control Points 等）。 | session_id (str) | success (bool)<br>topology (dict): {topology, patch_control_points?, restart_enabled?}<br>error_message (str, 可选) |
| rd.pipeline.get_viewports_scissors | 获取 Viewports 与 Scissors（多 viewport 支持）。 | session_id (str) | success (bool)<br>viewports (list[dict]): {x,y,w,h,min_depth,max_depth}<br>scissors (list[dict]): {x,y,w,h}<br>error_message (str, 可选) |
| rd.pipeline.get_rasterizer_state | 获取 Rasterizer State（cull mode、front face、depth clamp、depth bias、line width 等）。 | session_id (str) | success (bool)<br>rasterizer (dict)<br>error_message (str, 可选) |
| rd.pipeline.get_multisample_state | 获取 MSAA/coverage/alpha-to-coverage 等多采样相关状态。 | session_id (str) | success (bool)<br>multisample (dict)<br>error_message (str, 可选) |
| rd.pipeline.get_blend_state | 获取 Blend State（per RT 的 blend enable、因子、op、write mask）。 | session_id (str) | success (bool)<br>blend (dict)<br>error_message (str, 可选) |
| rd.pipeline.get_depth_stencil_state | 获取 Depth/Stencil State（depth func/write、stencil ops、stencil ref、bounds 等）。 | session_id (str) | success (bool)<br>depth_stencil (dict)<br>error_message (str, 可选) |
| rd.pipeline.get_output_targets | 获取 Output Merger / Framebuffer：RT/DS 绑定、formats、mip/slice、load/store（若可得）。 | session_id (str) | success (bool)<br>framebuffer (dict): {render_targets:[], depth_target?, samples?, formats?}<br>error_message (str, 可选) |
| rd.pipeline.get_render_targets | 仅获取 Render Targets 列表（包含 resource_id、format、mip/slice、name）。 | session_id (str) | success (bool)<br>render_targets (list[dict])<br>error_message (str, 可选) |
| rd.pipeline.get_depth_target | 仅获取 Depth/Stencil Target 信息。 | session_id (str) | success (bool)<br>depth_target (dict, 可选): {resource_id, format, mip, slice, name}<br>error_message (str, 可选) |
| rd.pipeline.get_resource_bindings | 获取指定 stage 的资源绑定（SRV/CBV/RO-Images 等）。 | session_id (str)<br>stage (str): 'VS'|'HS'|'DS'|'GS'|'PS'|'CS'|'AS'|'MS'|'RT' | success (bool)<br>bindings (list[dict]): {bindpoint, space?, type, resource_id, view?, name?, format?}<br>error_message (str, 可选) |
| rd.pipeline.get_uav_bindings | 获取指定 stage 的 UAV/Storage 绑定。 | session_id (str)<br>stage (str) | success (bool)<br>uavs (list[dict]): {bindpoint, space?, resource_id, view?, name?}<br>error_message (str, 可选) |
| rd.pipeline.get_sampler_bindings | 获取指定 stage 的 Sampler 绑定。 | session_id (str)<br>stage (str) | success (bool)<br>samplers (list[dict]): {bindpoint, space?, filter, address_u/v/w, lod_bias?, max_aniso?, comparison?}<br>error_message (str, 可选) |
| rd.pipeline.get_constant_buffers | 获取指定 stage 的 Constant Buffer 绑定（slot/buffer/offset/size）。 | session_id (str)<br>stage (str)<br>include_contents (bool, 可选, 默认 false): 若 true 则额外解码变量值（可能较慢）<br>max_bytes (int, 可选, 默认 1048576): 解码上限，避免超大 CB 导致内存压力 | success (bool)<br>constant_buffers (list[dict]): {slot, resource_id, offset, size, vars?}<br>error_message (str, 可选) |
| rd.pipeline.get_push_constants | 获取 Vulkan Push Constants（按 range/offset 组织；若当前 API 非 Vulkan 则返回空）。 | session_id (str)<br>decode_as (str, 可选): 'raw'|'u32'|'f32'|'struct'（struct 需要 layout）<br>layout (dict, 可选): struct layout 定义 | success (bool)<br>push_constants (list[dict]): {stage_flags, offset, size, data}<br>error_message (str, 可选) |
| rd.pipeline.get_dynamic_state | 获取 Vulkan Dynamic State（viewport/scissor/lineWidth/depthBias/stencilRef 等）。 | session_id (str) | success (bool)<br>dynamic_state (dict)<br>error_message (str, 可选) |
| rd.pipeline.get_root_signature | 获取 D3D12 Root Signature 摘要（参数表、descriptor ranges、static samplers）。 | session_id (str) | success (bool)<br>root_signature (dict, 可选)<br>error_message (str, 可选) |
| rd.pipeline.get_descriptor_heaps | 获取 D3D12 Descriptor Heaps 绑定信息（CBV/SRV/UAV, Sampler）。 | session_id (str) | success (bool)<br>descriptor_heaps (dict, 可选)<br>error_message (str, 可选) |
| rd.pipeline.get_resource_states | 获取 D3D12 Resource State（barrier 后的状态快照；若可得）。 | session_id (str)<br>resource_id (str, 可选): 指定资源；缺省则返回当前关键绑定资源的状态 | success (bool)<br>resource_states (list[dict]): {resource_id, subresource?, state, last_transition_event?}<br>error_message (str, 可选) |
| rd.pipeline.get_shader | 获取指定 stage 的 shader 句柄与摘要信息（resource_id、entry、debug info 可用性等）。 | session_id (str)<br>stage (str) | success (bool)<br>shader (dict, 可选): {resource_id, entry, name, shader_model?, has_debug_info?}<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.resource.list_all | 列出捕获中所有资源（Textures/Buffers/Shaders/Pipelines 等）。 | session_id (str)<br>types (list[str], 可选): 过滤类型，如 ['Texture','Buffer','Shader']；缺省为全部<br>include_unused (bool, 可选, 默认 true) | success (bool)<br>resources (list[dict]): {resource_id, type, name, byte_size?, format?, dims?}<br>error_message (str, 可选) |
| rd.resource.list_textures | 列出所有纹理资源，包含维度、格式、array、mips、samples。 | session_id (str)<br>include_render_targets (bool, 可选, 默认 true)<br>include_depth_stencil (bool, 可选, 默认 true) | success (bool)<br>textures (list[dict]): {resource_id, name, width,height,depth, mips, array_size, format, samples, usage_flags?}<br>error_message (str, 可选) |
| rd.resource.list_buffers | 列出所有 buffer 资源，包含大小、用途、结构化信息（若可得）。 | session_id (str) | success (bool)<br>buffers (list[dict]): {resource_id, name, byte_size, usage_flags?, stride?, structured?}<br>error_message (str, 可选) |
| rd.resource.get_details | 获取资源详细信息，根据资源类型返回不同字段。 | session_id (str)<br>resource_id (str) | success (bool)<br>details (dict): 通用字段 {type, name, creation?, current_state?} + 纹理/缓冲特有字段<br>error_message (str, 可选) |
| rd.resource.get_usage | 获取资源的使用情况，即哪些 event 读/写/绑定该资源。 | session_id (str)<br>resource_id (str)<br>max_events (int, 可选, 默认 20000) | success (bool)<br>usage (list[dict]): `{event_id, usage_type:'Read' |
| rd.resource.get_history | 获取资源的读写历史，按时间排序，包含写入来源、清除、copy 等。 | session_id (str)<br>resource_id (str)<br>include_reads (bool, 可选, 默认 false): 若为 true 则包含 reads（可能很长）<br>include_writes (bool, 可选, 默认 true) | success (bool)<br>history (list[dict]): {event_id, op, stage?, src_resource_id?, dst_resource_id?, detail?}<br>error_message (str, 可选) |
| rd.resource.get_initial_contents | 获取资源初始内容（如果捕获保存了初始内容），通常用于追踪“初始数据是否正确”。 | session_id (str)<br>resource_id (str)<br>output_path (str, 可选): 将数据保存到指定文件；缺省由系统分配 | success (bool)<br>artifact_path (str, 可选): 初始内容的二进制/纹理文件路径<br>format_hint (str, 可选): `'raw' |
| rd.resource.get_current_contents | 读取资源在当前 active event 时刻的内容（buffer 返回 bytes；texture 返回 subresource blob 或保存为文件）。 | session_id (str)<br>resource_id (str)<br>subresource (dict, 可选): 纹理子资源 {mip, slice, sample}；buffer 可省略<br>range (dict, 可选): buffer 范围 {offset, size}<br>output_path (str, 可选) | success (bool)<br>artifact_path (str, 可选)<br>byte_size (int, 可选)<br>error_message (str, 可选) |
| rd.resource.set_alias | 为资源设置一个本地别名，仅影响后续输出/报告，不修改捕获内容。 | session_id (str)<br>resource_id (str)<br>alias (str): 例如 'GBufferA'/'MainColor' 等 | success (bool)<br>error_message (str, 可选) |
| rd.resource.rename | 兼容工具：等价于 rd.resource.set_alias。 | session_id (str)<br>resource_id (str)<br>new_name (str) | success (bool)<br>error_message (str, 可选) |
| rd.resource.get_descriptor_info | 获取资源在当前绑定上下文中的 descriptor/view 信息（SRV/UAV/RTV/DSV 等）。 | session_id (str)<br>resource_id (str)<br>event_id (int, 可选): 若提供则临时切换到该 event 查询（不改变全局 active event） | success (bool)<br>descriptors (list[dict]): {usage, stage?, bindpoint?, view_format?, swizzle?, mip_range?, slice_range?}<br>error_message (str, 可选) |
| rd.resource.estimate_memory | 估算资源占用内存，按格式/维度推算，用于快速筛选大资源。 | session_id (str)<br>resource_id (str) | success (bool)<br>estimated_bytes (int)<br>breakdown (dict, 可选): {mip0, mips_total, metadata?}<br>error_message (str, 可选) |
| rd.resource.get_creation_context | 获取资源创建上下文：创建 event、创建参数摘要（若可得）。 | session_id (str)<br>resource_id (str) | success (bool)<br>creation (dict, 可选): {create_event_id, create_call?, params?, initial_state?}<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.texture.get_data | 用于读取纹理子资源的原始数据。并默认返回文件保存路径以避免 JSON 响应过大。 | session_id (str)<br>texture_id (str): 纹理资源 ID<br>subresource (dict, 可选): 包含 mip (int, 默认 0), slice (int, 默认 0), sample (int, 默认 0)<br>format (str, 可选): 输出格式，可选值为 'raw', 'dds', 'exr', 'png' (如果支持)<br>output_path (str, 可选) | success (bool)<br>artifact_path (str): 输出文件路径<br>byte_size (int, 可选)<br>error_message (str, 可选) |
| rd.texture.get_subresource_data | 用于读取并返回纹理子数据的元数据 (metadata)，例如尺寸、行距 (row pitch) 和格式 (format) 等，并支持选择性导出。 | session_id (str)<br>texture_id (str)<br>mip (int, 可选, 默认 0)<br>slice (int, 可选, 默认 0)<br>sample (int, 可选, 默认 0)<br>export (bool, 可选, 默认 false)<br>output_path (str, 可选) | success (bool)<br>subresource_info (dict): 包含 {width, height, depth, format, row_pitch, slice_pitch, samples}<br>artifact_path (str, 可选)<br>error_message (str, 可选) |
| rd.texture.get_pixel_value | 用于读取纹理在指定像素坐标处的值。该值将自动按照纹理格式解码为 float4 或 int4。 | session_id (str)<br>texture_id (str)<br>x (int)<br>y (int)<br>mip (int, 可选, 默认 0)<br>slice (int, 可选, 默认 0)<br>sample (int, 可选, 默认 0)<br>as_type (str, 可选, 默认 'float'): 返回值类型，可选 'float', 'uint', 'int' | success (bool)<br>value (`list[float |
| rd.texture.get_region_values | 用于读取纹理中一个矩形区域内的像素值，常用于局部统计分析、查找 NaN 或 Inf 值。 | session_id (str)<br>texture_id (str)<br>rect (dict): 包含 {x, y, w, h}<br>mip (int, 可选, 默认 0)<br>slice (int, 可选, 默认 0)<br>sample (int, 可选, 默认 0)<br>stride (int, 可选, 默认 1): 采样步长，大于 1 可降低处理成本<br>as_type (str, 可选, 默认 'float') | success (bool)<br>values (`list[list[float |
| rd.texture.get_pixel_history | 用于获取当前活动事件 (active event) 下某个像素的历史信息，即哪些绘制调用 (drawcall) 或调度 (dispatch) 影响了该像素。 | session_id (str)<br>texture_id (str): 通常为当前渲染目标 (RT)<br>x (int)<br>y (int)<br>mip (int, 可选, 默认 0)<br>slice (int, 可选, 默认 0)<br>sample (int, 可选, 默认 0)<br>include_shaders (bool, 可选, 默认 true) | success (bool)<br>history (list[dict]): 包含 {event_id, action_name, passed_tests?, pre_mod, post_mod, shader?}<br>error_message (str, 可选) |
| rd.texture.get_min_max | 用于计算纹理子资源的最小值 (min) 和最大值 (max)。 | session_id (str)<br>texture_id (str)<br>mip (int, 可选, 默认 0)<br>slice (int, 可选, 默认 0)<br>sample (int, 可选, 默认 0)<br>channels (str, 可选, 默认 'rgba'): 指定通道，可选 'r', 'rg', 'rgb', 'rgba' | success (bool)<br>min (list[float])<br>max (list[float])<br>error_message (str, 可选) |
| rd.texture.get_histogram | 用于计算纹理子资源的直方图。 | session_id (str)<br>texture_id (str)<br>mip (int, 可选, 默认 0)<br>slice (int, 可选, 默认 0)<br>channels (str, 可选, 默认 'r')<br>bins (int, 可选, 默认 256)<br>range (dict, 可选): 包含 {min, max} (如果缺省则自动计算) | success (bool)<br>histogram (list[int])<br>bin_edges (list[float])<br>error_message (str, 可选) |
| rd.texture.compute_stats | 用于计算纹理的统计量，包括均值 (mean)、标准差 (stddev)、NaN/Inf 计数以及 0/1 占比等，以便进行快速的健全性检查 (sanity check)。 | session_id (str)<br>texture_id (str)<br>mip (int, 可选, 默认 0)<br>slice (int, 可选, 默认 0)<br>sample (int, 可选, 默认 0)<br>stride (int, 可选, 默认 1) | success (bool)<br>stats (dict): 包含 {mean:[...], std:[...], nan_count, inf_count, zero_ratio?, one_ratio?}<br>error_message (str, 可选) |
| rd.texture.render_overlay | 用于将叠加层 (overlay) 渲染到纹理上并导出，例如绘制调用高亮、线框、视口/裁剪 (viewport/scissor) 或过度绘制 (quad overdraw) 等。 | session_id (str)<br>texture_id (str)<br>overlay (str): 叠加层类型，可选 'None', 'Drawcall', 'Wireframe', 'ViewportScissor', 'Overdraw'<br>event_id (int, 可选): 叠加层关联的事件 ID<br>output_path (str, 可选)<br>image_format (str, 可选, 默认 'png') | success (bool)<br>image_path (str)<br>error_message (str, 可选) |
| rd.texture.save_to_file | 用于根据显示映射和通道选择，将纹理导出到文件 (支持 PNG/EXR/DDS 等格式)。 | session_id (str)<br>texture_id (str)<br>subresource (dict, 可选): 包含 {mip, slice, sample}<br>output_path (str)<br>file_format (str, 可选): 文件格式，可选 'png', 'exr', 'dds', 'jpg'<br>channels (str, 可选, 默认 'rgba')<br>remap (dict, 可选): 包含 {black_point, white_point, gamma, hdr_clamp?}<br>flip_y (bool, 可选, 默认 false) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.texture.save_mip_chain | 用于批量导出 mip chain，可选择将每个 mip 保存为单独文件或合并到指定目录。 | session_id (str)<br>texture_id (str)<br>output_dir (str)<br>file_format (str, 可选, 默认 'png')<br>slice (int, 可选, 默认 0) | success (bool)<br>saved_files (list[str])<br>error_message (str, 可选) |
| rd.texture.diff | 用于对比两张纹理 (要求尺寸相同且格式可转换)，并输出差异图以及各项指标，如峰值信噪比 (PSNR)、最大绝对误差 (max error) 和均方误差 (mean squared error)。 | session_id (str)<br>tex_a (dict): 包含 {texture_id, subresource?}<br>tex_b (dict): 包含 {texture_id, subresource?}<br>metric (list[str], 可选): 指标列表，可选 ['max_abs', 'mse', 'psnr']<br>output_path (str, 可选): 差异图输出路径 (PNG/EXR) | success (bool)<br>metrics (dict)<br>diff_image_path (str, 可选)<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.buffer.get_data | 读取 buffer 的原始字节数据（默认保存到文件以避免 JSON 过大）。 | session_id (str)<br>buffer_id (str)<br>offset (int, 可选, 默认 0)<br>size (int, 可选): 读取字节数；缺省为 buffer 末尾<br>output_path (str, 可选): 若提供则写入该文件，否则写入临时 artifact 文件<br>as_base64 (bool, 可选, 默认 false): 小数据可直接 base64 返回 | success (bool)<br>artifact_path (str, 可选)<br>base64 (str, 可选)<br>byte_size (int)<br>error_message (str, 可选) |
| rd.buffer.get_structured_data | 按结构化布局解码 buffer 内容（适用于 StructuredBuffer/SSBO/顶点数据等）。 | session_id (str)<br>buffer_id (str)<br>layout (dict): 结构体布局定义，例如 {stride, fields:[{name,type,offset,count?}]}<br>offset (int, 可选, 默认 0)<br>count (int, 可选): 解码元素个数<br>max_elements (int, 可选, 默认 4096) | success (bool)<br>elements (list[dict]): 每元素为 {field:value,...}<br>error_message (str, 可选) |
| rd.buffer.save_to_file | 将 buffer 内容导出到文件（raw/bin）。 | session_id (str)<br>buffer_id (str)<br>output_path (str)<br>offset (int, 可选, 默认 0)<br>size (int, 可选) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.buffer.search_pattern | 在 buffer 内搜索字节模式（适用于查找 magic number、NaN pattern、特定 id）。 | session_id (str)<br>buffer_id (str)<br>pattern (str): 支持 hex 字符串（如 'FF 00 ?? 7F'）或 base64<br>offset (int, 可选, 默认 0)<br>size (int, 可选)<br>max_results (int, 可选, 默认 256) | success (bool)<br>matches (list[int]): 命中位置 offset 列表<br>error_message (str, 可选) |
| rd.mesh.get_post_vs_data | 获取指定 drawcall 的 Post-VS 数据（位置/属性等）。 | session_id (str)<br>event_id (int, 可选): 缺省使用当前 active event<br>view (str, 可选, 默认 'vs_out'): 'vs_out'|'gs_out'|'ts_out'|'ms_out'<br>instance (int, 可选, 默认 0)<br>view_index (int, 可选, 默认 0): multiview/VR<br>max_vertices (int, 可选): 限制导出顶点数 | success (bool)<br>mesh_data (dict): {vertex_count, index_count?, attributes:[...], data_path? 或 inline?}<br>error_message (str, 可选) |
| rd.mesh.get_post_gs_data | 获取指定 drawcall 的 Post-GS 数据（若管线包含 GS）。 | session_id (str)<br>event_id (int, 可选)<br>instance (int, 可选, 默认 0)<br>max_primitives (int, 可选) | success (bool)<br>mesh_data (dict)<br>error_message (str, 可选) |
| rd.mesh.decode_vertex_data | 对指定 VB 按输入布局解码顶点属性。 | session_id (str)<br>vertex_buffer_id (str)<br>layout (dict): 输入布局定义 {stride, attributes:[{semantic/name, format, offset}]}<br>vertex_offset (int, 可选, 默认 0)<br>vertex_count (int, 可选, 默认 256)<br>base_vertex (int, 可选, 默认 0) | success (bool)<br>vertices (list[dict]): 每顶点 {attr:value,...}<br>error_message (str, 可选) |
| rd.mesh.decode_index_data | 解码 Index Buffer（16/32-bit）为 index 列表。 | session_id (str)<br>index_buffer_id (str)<br>format (str): 'R16_UINT'|'R32_UINT'<br>index_offset (int, 可选, 默认 0)<br>index_count (int, 可选, 默认 256)<br>base_vertex (int, 可选, 默认 0) | success (bool)<br>indices (list[int])<br>error_message (str, 可选) |
| rd.mesh.get_drawcall_mesh_config | 获取当前 drawcall 的 mesh 配置：使用哪些 VB/IB、topology、baseVertex、indexOffset、instanceCount 等。 | session_id (str)<br>event_id (int, 可选) | success (bool)<br>config (dict): {topology, num_vertices, num_indices, num_instances, base_vertex, index_offset, vb_bindings, ib_binding}<br>error_message (str, 可选) |
| rd.mesh.export | 将指定 drawcall 的 mesh 导出为标准格式（OBJ/PLY/GLTF，视实现支持）。 | session_id (str)<br>event_id (int, 可选)<br>format (str, 可选, 默认 'obj'): 'obj'|'ply'|'gltf'<br>output_path (str)<br>include_attributes (bool, 可选, 默认 true)<br>space (str, 可选, 默认 'postvs'): 'prevs'|'postvs' | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.mesh.get_mesh_preview | 渲染 mesh 预览图（类似 RenderDoc Mesh Viewer 的输出截图）。 | session_id (str)<br>event_id (int, 可选)<br>mode (str, 可选, 默认 'solid'): 'solid'|'wireframe'|'normals'|'uv'<br>output_path (str, 可选)<br>width (int, 可选, 默认 512)<br>height (int, 可选, 默认 512) | success (bool)<br>image_path (str)<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.shader.get_reflection | 获取指定 shader 的反射信息（输入输出签名、资源绑定、常量块布局等）。 | session_id (str)<br>shader_id (str): shader 资源 ID<br>include_bindings (bool, 可选, 默认 true)<br>include_constant_blocks (bool, 可选, 默认 true) | success (bool)<br>reflection (dict): {entry_points, inputs, outputs, resources, constant_blocks, samplers, ...}<br>error_message (str, 可选) |
| rd.shader.get_disassembly | 获取指定 shader 的反汇编/IR（如 DXBC/DXIL/spirv disasm）。 | session_id (str)<br>shader_id (str)<br>target (str, 可选): `'auto' | 'dxbc' |
| rd.shader.get_source | 获取 shader 的源码/调试信息（若捕获包含 debug info，例如 PDB/DWARF/source embedding）。 | session_id (str)<br>shader_id (str)<br>prefer_original (bool, 可选, 默认 true): 优先返回原始源码（若可得） | success (bool)<br>source (str, 可选)<br>files (list[dict], 可选): {path, contents?}（实现可选择仅返回索引）<br>error_message (str, 可选) |
| rd.shader.extract_binary | 提取 shader 的二进制 blob（DXIL/DXBC/SPIR-V 等），并保存为文件。 | session_id (str)<br>shader_id (str)<br>output_path (str, 可选)<br>container (str, 可选): `'auto' | 'dxil' |
| rd.shader.save_binary | 兼容工具：等价于 rd.shader.extract_binary（保留更直观的名称）。 | session_id (str)<br>shader_id (str)<br>output_path (str)<br>container (str, 可选) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.shader.list_entry_points | 列出 shader 容器中的 entry points（适用于 DXIL library/多 entry SPIR-V）。 | session_id (str)<br>shader_id (str) | success (bool)<br>entry_points (list[dict]): {name, stage, profile?, exports?}<br>error_message (str, 可选) |
| rd.shader.get_bindpoint_mapping | 获取 shader 资源绑定点映射（register/space/binding -> semantic meaning）。 | session_id (str)<br>shader_id (str) | success (bool)<br>mapping (list[dict]): {resource_name, bindpoint, space?, type, stage?}<br>error_message (str, 可选) |
| rd.shader.get_constant_block_layout | 读取常量块布局（variables、offset、type、array、matrix layout）。 | session_id (str)<br>shader_id (str)<br>block_name_or_index (str|int) | success (bool)<br>layout (dict): {name, byte_size, vars:[{name,type,offset,rows,cols,array_size?}]}<br>error_message (str, 可选) |
| rd.shader.get_constant_buffer_contents | 读取指定 stage/slot 的 Constant Buffer 并按反射结构解码变量值（推荐用于专家级核对）。 | session_id (str)<br>stage (str)<br>slot (int)<br>array_index (int, 可选, 默认 0): CB array/space 情况<br>max_bytes (int, 可选, 默认 1048576)<br>flatten (bool, 可选, 默认 true): 是否将嵌套结构摊平成 path->value | success (bool)<br>cbuffer (dict): {resource_id, offset, size, vars}（vars 为 {path:value} 或树）<br>error_message (str, 可选) |
| rd.shader.edit_and_replace | 在当前 Replay Session 中替换 shader（实时验证修复假设）。 | session_id (str)<br>stage (str): `'VS' | 'PS' |
| rd.shader.revert_replacement | 撤销 shader 替换，恢复捕获中的原始 shader。 | session_id (str)<br>replacement_id (str) | success (bool)<br>error_message (str, 可选) |
| rd.shader.list_replacements | 列出当前 session 中已生效的 shader 替换。 | session_id (str) | success (bool)<br>replacements (list[dict]): {replacement_id, stage, original_shader_id, created_event?, status, messages?}<br>error_message (str, 可选) |
| rd.shader.get_messages | 获取 RenderDoc 在 shader 编译/替换/调试过程产生的 messages。 | session_id (str)<br>severity_min (str, 可选, 默认 'info'): `'debug' | 'info' |
| rd.shader.compile | （可选扩展）在 server 侧调用外部编译器（dxc/glslangValidator/metal）编译 shader，返回 binary 路径；用于构建替换输入。 | source (str): HLSL/GLSL/MSL 源码<br>stage (str)<br>entry (str)<br>target (str): 例如 'vs_6_6'/'ps_6_6' 或 'spirv1.6'<br>defines (dict, 可选)<br>include_dirs (list[str], 可选)<br>additional_args (list[str], 可选)<br>output_path (str, 可选) | success (bool)<br>binary_path (str, 可选)<br>stderr (str, 可选)<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.shader.debug_start | 启动 shader debugger（默认 pixel），返回 debug session 句柄。 | session_id (str)<br>mode (str, 可选, 默认 'pixel'): 'pixel' | 'vertex' | 'compute'<br>event_id (int, 可选): 缺省为当前 active event<br>params (dict):<br>- pixel: {x, y, sample?, target?}<br>- vertex: {vertex_index, instance, view?}<br>- compute: {group_x, group_y, group_z, thread_x, thread_y, thread_z}<br>timeout_ms (int, 可选, 默认 10000) | success (bool)<br>shader_debug_id (str)<br>initial_state (dict): {pc, location?, variables_summary?}<br>error_message (str, 可选) |
| rd.shader.get_debug_state | 获取当前 debugger 状态（当前 PC、活跃函数、变量、寄存器等）。 | session_id (str)<br>shader_debug_id (str)<br>detail_level (str, 可选, 默认 'full'): 'summary' | 'full' | success (bool)<br>state (dict)<br>error_message (str, 可选) |
| rd.debug.step | 单步执行 debugger（步进一条指令或源代码行）。 | session_id (str)<br>shader_debug_id (str)<br>step_mode (str, 可选, 默认 'instruction'): 'instruction' | 'line' | 'over' | 'out' | success (bool)<br>state (dict): 更新后的状态摘要（pc/line/changed_vars）<br>error_message (str, 可选) |
| rd.debug.continue | 继续执行 debugger（直到断点/结束/超时）。 | session_id (str)<br>shader_debug_id (str)<br>timeout_ms (int, 可选, 默认 10000) | success (bool)<br>state (dict)<br>stopped_reason (str, 可选): 'breakpoint' | 'finished' | 'timeout' | 'error'<br>error_message (str, 可选) |
| rd.debug.run_to | 运行到指定位置（PC 或 source location）。 | session_id (str)<br>shader_debug_id (str)<br>target (dict): {pc?, file?, line?}<br>timeout_ms (int, 可选, 默认 10000) | success (bool)<br>state (dict)<br>error_message (str, 可选) |
| rd.debug.set_breakpoints | 设置断点列表（按 PC 或 source location）。 | session_id (str)<br>shader_debug_id (str)<br>breakpoints (list[dict]): {pc?, file?, line?} | success (bool)<br>active_breakpoints (list[dict])<br>error_message (str, 可选) |
| rd.debug.clear_breakpoints | 清除所有断点。 | session_id (str)<br>shader_debug_id (str) | success (bool)<br>error_message (str, 可选) |
| rd.debug.get_variables | 获取当前 scope 的变量（支持按名称过滤/按路径展开结构体）。 | session_id (str)<br>shader_debug_id (str)<br>name_filter (str, 可选): regex/contains<br>expand_depth (int, 可选, 默认 2)<br>max_variables (int, 可选, 默认 2048) | success (bool)<br>variables (list[dict]): {name, type, value, children?}<br>error_message (str, 可选) |
| rd.debug.evaluate_expression | 在 debugger 上下文中求值表达式（如 'cb0.myVar' 或寄存器表达式；实现可选）。 | session_id (str)<br>shader_debug_id (str)<br>expression (str) | success (bool)<br>value (any, 可选)<br>error_message (str, 可选) |
| rd.debug.get_callstack | 获取 shader 调用栈（函数名/文件/行）。 | session_id (str)<br>shader_debug_id (str) | success (bool)<br>callstack (list[dict]): {function, file?, line?, pc?}<br>error_message (str, 可选) |
| rd.debug.finish | 结束并释放 shader debug session。 | session_id (str)<br>shader_debug_id (str) | success (bool)<br>error_message (str, 可选) |
| rd.debug.pixel_history | 获取像素历史（推荐替代 rd.texture.get_pixel_history；支持更丰富的测试/输出解释字段）。 | session_id (str)<br>x (int)<br>y (int)<br>target (dict, 可选): {texture_id?}（缺省为当前 RT0）<br>sample (int, 可选, 默认 0)<br>include_tests (bool, 可选, 默认 true)<br>include_shader_outputs (bool, 可选, 默认 true) | success (bool)<br>history (list[dict])<br>error_message (str, 可选) |
| rd.debug.explain_test_failure | 对单条 pixel history 记录给出"为什么没写入/被丢弃"的解释（depth/stencil/scissor/blend 等）。 | session_id (str)<br>history_item (dict): 来自 rd.debug.pixel_history 的某个元素<br>verbosity (str, 可选, 默认 'normal'): 'short' | 'normal' | 'verbose' | success (bool)<br>explanation (str)<br>key_facts (dict, 可选): 结构化要点（depth test result 等）<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.perf.enumerate_counters | 枚举可用的 GPU counters。 | session_id (str) | success (bool)<br>counters (list[dict]): {counter_id, name, description?, unit?, category?}<br>error_message (str, 可选) |
| rd.perf.describe_counter | 获取单个 counter 的详细描述。 | session_id (str)<br>counter_id (int) | success (bool)<br>counter (dict): {counter_id, name, description, unit, result_type, uuid?}<br>error_message (str, 可选) |
| rd.perf.sample_counters | 对当前帧/指定 event 范围采样 counters（可能较慢）。 | session_id (str)<br>counter_ids (list[int])<br>event_range (dict, 可选): {start_event_id, end_event_id}；缺省为整帧<br>stride (int, 可选, 默认 1): 每隔多少个 event 采样一次（降低成本） | success (bool)<br>samples (list[dict]): {event_id, values:{counter_id:value}}<br>error_message (str, 可选) |
| rd.perf.get_event_durations | 获取每个 event 的 GPU duration（若支持 timing queries；可用于定位慢 draw）。 | session_id (str)<br>event_range (dict, 可选)<br>max_events (int, 可选, 默认 20000) | success (bool)<br>durations (list[dict]): {event_id, duration_ms, action_name?}<br>error_message (str, 可选) |
| rd.perf.get_frame_timing | 获取整帧 timing 摘要（GPU/CPU 若可得），含关键区间统计。 | session_id (str)<br>frame_index (int, 可选) | success (bool)<br>timing (dict): {frame_index, gpu_ms?, cpu_ms?, present_ms?, markers?}<br>error_message (str, 可选) |
| rd.perf.get_pipeline_statistics | 获取 pipeline statistics（VS/PS invocations、primitives、clipped 等；若 API/驱动支持）。 | session_id (str)<br>event_id (int, 可选) | success (bool)<br>stats (dict): {ia_vertices, ia_primitives, vs_invocations, ps_invocations, cs_invocations?, ...}<br>error_message (str, 可选) |
| rd.analysis.get_frame_stats | 获取帧级统计与警告摘要。 | session_id (str)<br>frame_index (int, 可选) | success (bool)<br>stats (dict): {drawcalls, dispatches, markers, resources, warnings?}<br>error_message (str, 可选) |
| rd.analysis.get_event_stats | 获取指定 event 的统计摘要（draw 参数、绑定资源数、RT/DS 格式、shader 信息等）。 | session_id (str)<br>event_id (int, 可选) | success (bool)<br>event_stats (dict)<br>error_message (str, 可选) |
| rd.analysis.get_warnings | 获取 RenderDoc 在回放/验证期间产生的 warnings/errors（如 undefined behavior、API misuse）。 | session_id (str)<br>severity_min (str, 可选, 默认 'warn') | success (bool)<br>warnings (list[dict]): {severity, message, event_id?}<br>error_message (str, 可选) |
| rd.analysis.estimate_overdraw | 估算 overdraw（实现可选：基于像素历史采样或 RenderDoc 内置 overdraw overlay）。 | session_id (str)<br>target (dict, 可选): {texture_id?}<br>region (dict, 可选): {x,y,w,h}<br>sampling (dict, 可选): {stride, max_samples}<br>output_path (str, 可选): 输出 heatmap 图片 | success (bool)<br>overdraw (dict): {avg, max, histogram?}<br>heatmap_path (str, 可选)<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.export.screenshot | 导出当前 event 的输出截图（等价于 UI 的保存 framebuffer）。 | session_id (str)<br>target (dict, 可选): {texture_id? , rt_index?}；缺省为当前 RT0<br>event_id (int, 可选): 指定 event 输出；缺省为当前 active event<br>output_path (str)<br>file_format (str, 可选, 默认 'png')<br>include_alpha (bool, 可选, 默认 true)<br>overlay (str, 可选): 'None'|'Drawcall'|'Wireframe'|'Overdraw' | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.export.texture | 导出纹理（快捷封装 rd.texture.save_to_file）。 | session_id (str)<br>texture_id (str)<br>subresource (dict, 可选)<br>output_path (str)<br>file_format (str, 可选, 默认 'png')<br>remap (dict, 可选) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.export.buffer | 导出 buffer 到文件（快捷封装 rd.buffer.save_to_file）。 | session_id (str)<br>buffer_id (str)<br>output_path (str)<br>offset (int, 可选, 默认 0)<br>size (int, 可选) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.export.mesh | 导出当前 drawcall 的 mesh（快捷封装 rd.mesh.export）。 | session_id (str)<br>event_id (int, 可选)<br>format (str, 可选, 默认 'obj')<br>output_path (str) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.export.pipeline_state_json | 将当前 event 的 pipeline state 以 JSON 导出到文件（便于 diff/归档）。 | session_id (str)<br>event_id (int, 可选)<br>output_path (str)<br>detail_level (str, 可选, 默认 'full') | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.export.event_tree_json | 导出当前帧的 event/draw 树到 JSON。 | session_id (str)<br>output_path (str)<br>max_depth (int, 可选) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.export.resource_list_csv | 导出资源列表到 CSV（包含名称/类型/大小/格式/维度等）。 | session_id (str)<br>output_path (str)<br>types (list[str], 可选) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.export.pixel_history_json | 导出某像素的 pixel history 到 JSON（便于共享/复现）。 | session_id (str)<br>x (int)<br>y (int)<br>target (dict, 可选)<br>output_path (str) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.export.shader_bundle | 导出当前 event 相关 shader 的 bundle（反射 + disasm + binary），用于离线分析。 | session_id (str)<br>event_id (int, 可选)<br>output_dir (str)<br>include_binaries (bool, 可选, 默认 true)<br>include_reflection (bool, 可选, 默认 true)<br>include_disassembly (bool, 可选, 默认 true) | success (bool)<br>saved_files (list[str])<br>error_message (str, 可选) |
| rd.export.cbuffer_dump | 导出当前 event 的常量缓冲数据（按 stage/slot 命名），支持结构化变量值。 | session_id (str)<br>output_dir (str)<br>stages (list[str], 可选): 缺省为所有 stage<br>include_raw (bool, 可选, 默认 false)<br>include_decoded (bool, 可选, 默认 true) | success (bool)<br>saved_files (list[str])<br>error_message (str, 可选) |
| rd.export.repro_bundle_zip | 生成可复现的 bug report 压缩包：包含 rdc、关键截图、pipeline JSON、pixel history、shader bundle、cbuffer dump 等。 | session_id (str)<br>bundle_spec (dict): {include_rdc?, include_thumbnails?, include_pipeline_json?, include_pixel_history?, include_shaders?, include_cbuffers?, include_resources?}<br>focus (dict, 可选): {event_id?, x?, y?, target_texture_id?}<br>output_path (str) | success (bool)<br>zip_path (str)<br>manifest (dict, 可选): 打包内容清单<br>error_message (str, 可选) |
| rd.export.markdown_report | 生成 Markdown 报告（包含帧摘要、关键 pass、问题点、截图链接、数据引用）。 | session_id (str)<br>report_spec (dict, 可选): {title?, include_sections?, verbosity?}<br>focus (dict, 可选)<br>output_path (str) | success (bool)<br>report_path (str)<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.remote.connect | 连接到远程 RenderDoc server（RDC remote server / target control）。 | host (str)<br>port (int, 可选, 默认 38920)<br>timeout_ms (int, 可选, 默认 5000) | success (bool)<br>remote_id (str)<br>server_info (dict): {version, name?, platform?, capabilities?}<br>error_message (str, 可选) |
| rd.remote.disconnect | 断开远程连接。 | remote_id (str) | success (bool)<br>error_message (str, 可选) |
| rd.remote.ping | 检查远程连接可用性与延迟。 | remote_id (str) | success (bool)<br>latency_ms (float, 可选)<br>error_message (str, 可选) |
| rd.remote.list_targets | 列出远程可连接的 targets（已注入/已运行应用、可抓帧进程等）。 | remote_id (str) | success (bool)<br>targets (list[dict]): {target_id, name, pid?, api?, status}<br>error_message (str, 可选) |
| rd.remote.launch_app | 在远程机器启动应用并注入 RenderDoc（若 server 支持）。 | remote_id (str)<br>exe_path (str)<br>working_dir (str, 可选)<br>cmdline (str, 可选)<br>env (dict, 可选)<br>capture_options (dict, 可选): 参考 rd.remote.set_capture_options | success (bool)<br>target_id (str, 可选)<br>pid (int, 可选)<br>error_message (str, 可选) |
| rd.remote.set_capture_options | 设置远程抓帧参数（保存路径、frame limit、禁止/允许某些 API 特性等）。 | remote_id (str)<br>options (dict): {capture_file_path_template?, capture_all_cmd_lists?, allow_vsync?, hook_into_children?, ref_all_resources?, ...} | success (bool)<br>applied (dict)<br>error_message (str, 可选) |
| rd.remote.set_overlay_options | 设置远程 overlay（例如显示 capture 提示、frame number、FPS）。 | remote_id (str)<br>options (dict): {enabled, show_frame_number?, show_fps?, show_capture_list?} | success (bool)<br>error_message (str, 可选) |
| rd.remote.trigger_capture | 在远程 target 上触发一次或多次捕获。 | remote_id (str)<br>target_id (str, 可选): 缺省表示 server 默认 target（若存在）<br>num_frames (int, 可选, 默认 1)<br>capture_delay_ms (int, 可选, 默认 0): 延迟触发 | success (bool)<br>captures (list[dict]): {capture_id, capture_path, timestamp}<br>error_message (str, 可选) |
| rd.remote.queue_capture | 在远程 target 上排队捕获（例如下一次 Present/下一帧），并返回队列状态。 | remote_id (str)<br>target_id (str, 可选)<br>num_frames (int, 可选, 默认 1) | success (bool)<br>queue_status (dict): {queued, pending, last_capture_id?}<br>error_message (str, 可选) |
| rd.remote.list_captures | 列出远程服务器上所有可用捕获。 | remote_id (str) | success (bool)<br>captures (list[dict]): {capture_id, timestamp, api, local_path?}<br>error_message (str, 可选) |
| rd.remote.copy_capture | 将远程 capture 拷贝到本地路径（也可用于下载）。 | remote_id (str)<br>capture_id (int)<br>local_path (str) | success (bool)<br>saved_path (str)<br>error_message (str, 可选) |
| rd.remote.delete_capture | 删除远程服务器上的 capture（谨慎使用）。 | remote_id (str)<br>capture_id (int) | success (bool)<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.app.is_available | 检查目标进程是否暴露 RenderDoc In-Application API（或该 MCP server 是否已与应用建立 IPC）。 | connection (dict, 可选): {pid?, endpoint?, target_id?} | success (bool)<br>available (bool)<br>detail (dict, 可选)<br>error_message (str, 可选) |
| rd.app.start_frame_capture | 在应用侧开始捕获（等价于 renderdoc_app.h 的 StartFrameCapture）。 | connection (dict): {pid? 或 endpoint?}<br>device (str, 可选): 设备/上下文标识（多 device 情况） | success (bool)<br>error_message (str, 可选) |
| rd.app.end_frame_capture | 在应用侧结束捕获并生成 rdc 文件（等价于 EndFrameCapture）。 | connection (dict): {pid? 或 endpoint?}<br>output_path (str, 可选): 捕获文件保存路径模板/目录 | success (bool)<br>capture_path (str, 可选)<br>error_message (str, 可选) |
| rd.app.trigger_capture | 在应用侧触发一次 capture（start+end 封装），可用于脚本化抓帧。 | connection (dict)<br>num_frames (int, 可选, 默认 1)<br>output_path (str, 可选) | success (bool)<br>capture_paths (list[str], 可选)<br>error_message (str, 可选) |
| rd.app.set_capture_option | 设置应用侧 capture options（如 allow_vsync、ref_all_resources、debug_device 等）。 | connection (dict)<br>option (str): 选项名<br>value (bool|int|float|str) | success (bool)<br>error_message (str, 可选) |
| rd.app.get_capture_options | 读取应用侧当前 capture options。 | connection (dict) | success (bool)<br>options (dict)<br>error_message (str, 可选) |
| rd.app.push_marker | 在应用侧 push marker/region（等价于 StartFrameCapture 之外的 marker API；用于让 event 树更可读）。 | connection (dict)<br>name (str) | success (bool)<br>error_message (str, 可选) |
| rd.app.pop_marker | 在应用侧 pop marker/region。 | connection (dict) | success (bool)<br>error_message (str, 可选) |
| rd.app.set_marker | 在应用侧插入单点 marker（即时 marker）。 | connection (dict)<br>name (str) | success (bool)<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.diag.scan_common_issues | 对当前 active event 执行一组常见问题扫描（空绑定、viewport/scissor、depth/blend、格式不匹配等），输出结构化 issue 列表。 | session_id (str)<br>severity_min (str, 可选, 默认 'info')<br>include_suggestions (bool, 可选, 默认 true) | success (bool)<br>issues (list[dict]): {severity, category, message, evidence?, suggestion?}<br>error_message (str, 可选) |
| rd.diag.check_render_targets | 检查 RT 绑定/format/write mask 是否合理（例如 RT0 为空、format 与 shader 输出不兼容）。 | session_id (str)<br>expect (dict, 可选): 期望约束，例如 {min_rt_count?, formats?, require_alpha?} | success (bool)<br>issues (list[dict])<br>error_message (str, 可选) |
| rd.diag.check_depth_stencil | 检查 depth/stencil 状态导致的“全被丢弃/全写深度失败”等问题。 | session_id (str)<br>pixel (dict, 可选): {x,y} 若提供则结合 pixel history 给出更强解释 | success (bool)<br>issues (list[dict])<br>error_message (str, 可选) |
| rd.diag.check_viewport_scissor | 检查 viewport/scissor 是否为空/越界/与 RT 尺寸不匹配。 | session_id (str) | success (bool)<br>issues (list[dict])<br>error_message (str, 可选) |
| rd.diag.check_culling | 检查 cull mode/front face/depth clip 导致的“看不见几何体”。 | session_id (str)<br>mesh_preview (bool, 可选, 默认 false): 若 true 则附带 mesh preview 截图路径 | success (bool)<br>issues (list[dict])<br>artifacts (list[str], 可选)<br>error_message (str, 可选) |
| rd.diag.check_blend | 检查 blend state（例如全 0 写出、写 mask 关闭、premul 错误等）。 | session_id (str)<br>rt_index (int, 可选, 默认 0) | success (bool)<br>issues (list[dict])<br>error_message (str, 可选) |
| rd.diag.check_srgb | 检查 sRGB/linear mismatch（RT 格式、采样视图、gamma 映射）。 | session_id (str)<br>textures (list[str], 可选): 指定需要检查的 texture_id 列表 | success (bool)<br>issues (list[dict])<br>error_message (str, 可选) |
| rd.diag.check_resource_bindings | 检查资源绑定：null/未初始化、dimension 不匹配、UAV/SRV 冲突、descriptor 越界等（按 API 能力）。 | session_id (str)<br>stages (list[str], 可选): 缺省为所有 stage<br>include_descriptor_dump (bool, 可选, 默认 false) | success (bool)<br>issues (list[dict])<br>descriptor_dump (dict, 可选)<br>error_message (str, 可选) |
| rd.diag.check_constant_buffers | 检查常量缓冲：slot 为空、size 不足、offset 非对齐、变量出现 NaN/Inf（若解码）。 | session_id (str)<br>stages (list[str], 可选)<br>decode_values (bool, 可选, 默认 true) | success (bool)<br>issues (list[dict])<br>error_message (str, 可选) |
| rd.diag.check_d3d12_resource_states | （D3D12 专用）检查关键资源在当前 event 的 resource state 是否与用途一致（heuristic）。 | session_id (str)<br>resources (list[str], 可选): 指定资源；缺省为当前绑定资源集合 | success (bool)<br>issues (list[dict])<br>error_message (str, 可选) |
| rd.diag.check_vk_dynamic_state | （Vulkan 专用）检查 dynamic state 是否遗漏设置（例如 viewport/scissor 未设置导致空渲染）。 | session_id (str) | success (bool)<br>issues (list[dict])<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.macro.summarize_frame | 生成整帧的专家级摘要：按 Pass/Marker 归类 drawcalls，列出主要 RT/DS、关键 shader、耗时热点（若可得）。 | session_id (str)<br>frame_index (int, 可选): 缺省当前帧<br>max_passes (int, 可选, 默认 50)<br>max_draws_per_pass (int, 可选, 默认 50)<br>include_timings (bool, 可选, 默认 true) | success (bool)<br>summary (dict): {passes:[...], main_outputs:[...], hotspots:[...], warnings:[...]}<br>trace (list[str], 可选): 该宏内部调用的关键步骤摘要<br>error_message (str, 可选) |
| rd.macro.generate_pass_summary | 对指定 marker/pass 范围生成摘要（输出/输入资源、drawcall 列表、统计）。 | session_id (str)<br>pass_range (dict): {begin_event_id, end_event_id}<br>include_resource_lists (bool, 可选, 默认 true)<br>include_state_changes (bool, 可选, 默认 false) | success (bool)<br>pass_summary (dict)<br>error_message (str, 可选) |
| rd.macro.find_pass_by_marker | 按 marker 名称/regex 查找 pass，并返回对应 event 范围（适合快速定位某个渲染阶段）。 | session_id (str)<br>name_regex (str)<br>max_results (int, 可选, 默认 20) | success (bool)<br>passes (list[dict]): {name, begin_event_id, end_event_id, path?}<br>error_message (str, 可选) |
| rd.macro.locate_draw_affecting_pixel | 给定屏幕像素 (x,y)，定位影响该像素的 draw/dispatch 列表（排序：最近修改优先；可过滤通过 depth/stencil 的记录）。 | session_id (str)<br>x (int)<br>y (int)<br>target (dict, 可选): {texture_id? , rt_index?}<br>policy (dict, 可选): {only_passed_tests?:bool, include_failed?:bool, max_items?:int} | success (bool)<br>candidates (list[dict]): {event_id, action_name, contribution, passed_tests?, pre, post}<br>error_message (str, 可选) |
| rd.macro.explain_pixel | 对某像素给出“专家解释”：按 pixel history 还原每次写入的 depth/stencil/blend 路径，并指出最终颜色来自哪一步。 | session_id (str)<br>x (int)<br>y (int)<br>target (dict, 可选)<br>verbosity (str, 可选, 默认 'normal'): `'short' | 'normal' |
| rd.macro.trace_resource_lifetime | 追踪资源生命周期：创建 -> 初始化 -> 关键写入 -> 关键读取（按 pass 归纳），用于定位“某资源何时被写坏”。 | session_id (str)<br>resource_id (str)<br>include_reads (bool, 可选, 默认 false)<br>group_by_pass (bool, 可选, 默认 true)<br>max_events (int, 可选, 默认 50000) | success (bool)<br>lifetime (dict): {resource, creation, writes, reads?, by_pass?}<br>error_message (str, 可选) |
| rd.macro.resource_dependency_graph | 构建资源依赖图：节点为资源，边表示 copy/resolve/render-to/read-from（用于复杂 frame 的因果分析）。 | session_id (str)<br>event_range (dict, 可选): {start_event_id,end_event_id}<br>max_nodes (int, 可选, 默认 2000)<br>max_edges (int, 可选, 默认 10000) | success (bool)<br>graph (dict): {nodes:[{id,name,type}], edges:[{src,dst,op,event_id}] }<br>error_message (str, 可选) |
| rd.macro.find_state_change_point | 在 event 区间内搜索“某个状态何时从 A 变为 B”（通过 pipeline diff 快速二分/线性扫描）。 | session_id (str)<br>event_range (dict): {start_event_id,end_event_id}<br>state_path (str): 例如 'OM.Blend.RT0.Enable' 或 'PS.Shader'<br>target_value (any): 期望值<br>search_policy (str, 可选, 默认 'binary'): `'binary' | 'linear'<br>max_steps (int, 可选, 默认 64`) |
| rd.macro.compare_events_report | 对两个 event 生成可读的对比报告：state diff + 绑定资源 diff + shader diff（含链接到导出的 artifacts）。 | session_id (str)<br>event_a (int)<br>event_b (int)<br>output_path (str, 可选): 若提供则写入 Markdown 报告 | success (bool)<br>report (str, 可选): 若未写文件则直接返回 Markdown 文本<br>report_path (str, 可选)<br>error_message (str, 可选) |
| rd.macro.find_unexpected_clear | 扫描帧内对关键 RT/DS 的 clear/Discard 操作，定位“画面被清掉/闪烁”的来源。 | session_id (str)<br>targets (list[dict], 可选): [{texture_id, name?}]；缺省为当前主要 RT/DS<br>max_results (int, 可选, 默认 200) | success (bool)<br>clears (list[dict]): {event_id, action_name, target, clear_value, type}<br>error_message (str, 可选) |
| rd.macro.find_nan_inf_in_targets | 在指定输出纹理中查找 NaN/Inf（可采样或全量扫描），输出命中坐标与局部截图。 | session_id (str)<br>texture_id (str, 可选): 缺省为当前 RT0<br>scan (dict, 可选): {stride:int=4, max_hits:int=200, region?}<br>output_dir (str, 可选) | success (bool)<br>hits (list[dict]): {x,y,value}<br>artifacts (list[str], 可选)<br>error_message (str, 可选) |
| rd.macro.quick_triage_missing_draw | 快速排查“某 draw/物体不见了”：结合 mesh preview、pipeline state、viewport/scissor、depth/cull/blend、binding sanity，输出结论与证据。 | session_id (str)<br>event_id (int, 可选): 缺省当前 active event<br>pixel (dict, 可选): {x,y} 若提供则结合 pixel history<br>output_dir (str, 可选): 输出截图/JSON 的目录 | success (bool)<br>triage (dict): {likely_root_causes:[...], evidence:[...], next_actions:[...]}<br>artifacts (list[str], 可选)<br>error_message (str, 可选) |
| rd.macro.build_bug_report_pack | 更高层的 bug report 打包：调用 rd.export.repro_bundle_zip 并补充解释文本、关键 diff、资源依赖图。 | session_id (str)<br>focus (dict, 可选)<br>output_path (str)<br>verbosity (str, 可选, 默认 'normal') | success (bool)<br>zip_path (str)<br>summary (str, 可选): 报告摘要（可直接贴到 issue）<br>error_message (str, 可选) |
| rd.macro.shader_hotfix_validate | 在 session 内做“替换 shader -> 重新导出对比截图/指标”的闭环验证，用于快速验证修复假设。 | session_id (str)<br>replacement (dict): 参考 rd.shader.edit_and_replace<br>validation (dict): {target_texture_id?, x?, y?, metric? , screenshot_before?, screenshot_after?}<br>output_dir (str, 可选) | success (bool)<br>result (dict): {replacement_id, metrics?, before_path?, after_path?, diff_path?}<br>error_message (str, 可选) |


| 工具名称 | 功能描述 | 主要参数 | 返回结果 |
| rd.util.compute_hash | 计算文件或字节的哈希值（默认为 sha256），主要用于验证导出内容的一致性。 | path (str): 文件路径<br>algo (str, 可选, 默认 'sha256'): 哈希算法 | success (bool): 操作是否成功<br>hash (str): 计算出的哈希值<br>error_message (str, 可选): 错误信息（如果存在） |
| rd.util.diff_text | 对比两段文本或两个文件，并输出统一差异格式 (unified diff)，这对于比较 shader disasm 或 pipeline JSON 等内容非常有用。 | a (str): 文本内容或文件路径<br>b (str): 文本内容或文件路径<br>a_is_path (bool, 可选, 默认 false): 标记 a 是否为文件路径<br>b_is_path (bool, 可选, 默认 false): 标记 b 是否为文件路径<br>context_lines (int, 可选, 默认 3): 上下文行数 | success (bool): 操作是否成功<br>diff (str): 差异内容<br>error_message (str, 可选): 错误信息（如果存在） |
| rd.util.diff_images | 对比两张图片（支持 PNG、JPEG、EXR 格式），输出差异图及相关指标，如 PSNR、SSIM 和最大绝对误差。 | image_a_path (str): 第一张图片的文件路径<br>image_b_path (str): 第二张图片的文件路径<br>metrics (list[str], 可选): 需要计算的指标列表，例如 ['psnr', 'ssim', 'max_abs', 'mse']<br>output_path (str, 可选): 差异图的输出路径 | success (bool): 操作是否成功<br>metrics (dict): 计算出的指标结果<br>diff_image_path (str, 可选): 差异图片路径（如果生成）<br>error_message (str, 可选): 错误信息（如果存在） |
| rd.util.pack_zip | 将一组文件打包成 zip 压缩文件。此为通用工具，导出功能已提供专用打包接口。 | paths (list[str]): 需要打包的文件路径列表<br>output_path (str): zip 文件的输出路径 | success (bool): 操作是否成功<br>zip_path (str): 生成的 zip 文件路径<br>error_message (str, 可选): 错误信息（如果存在） |
| rd.util.list_artifacts | 列出当前会话生成的所有 artifacts，便于后续清理或归档。 | session_id (str, 可选): 会话 ID，缺省时列出全局 artifacts<br>prefix (str, 可选): 用于过滤 artifacts 路径的前缀 | success (bool): 操作是否成功<br>artifacts (list[dict]): artifacts 列表，包含 {path, byte_size, mime?, created_ts?} 等信息<br>error_message (str, 可选): 错误信息（如果存在） |
| rd.util.cleanup_artifacts | 根据指定条件（如会话、前缀、时间或大小阈值）清理 artifacts。 | session_id (str, 可选): 会话 ID<br>older_than_ms (int, 可选): 清理早于指定毫秒数的 artifacts<br>prefix (str, 可选): 用于过滤 artifacts 路径的前缀<br>max_total_bytes (int, 可选): 清理 artifacts 直到总大小低于指定字节数 | success (bool): 操作是否成功<br>deleted (list[str]): 被删除的 artifacts 路径列表<br>freed_bytes (int, 可选): 释放的字节数<br>error_message (str, 可选): 错误信息（如果存在） |
