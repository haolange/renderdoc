{
  "mcpServers": {
    "rdx-mcp": {
      "command": "python",
      "args": ["-m", "rdx.server"],
      "description": "RenderDoc MCP Server — 提供 rd.* 工具集（rd.capture、rd.pipeline、rd.texture、rd.shader 等）"
    }
  }
}
